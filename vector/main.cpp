#include <vector>
#include <iostream>
#include "ft_vector.hpp"

int main() {
	ft::vector <int> v(10, 42);
	// std::cout << v.size() << " " << v.capacity() << std::endl;
	// for (int i = 0; i < 12; ++i) {
	// 	v.push_back(i);
	// 	std::cout << v.size() << " " << v.capacity() << std::endl;
	// }
}